package dev.jkiakumbo.paymentorchestrator.orchestratorservice.config

import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration

@Configuration
class TracingConfig {
    // Tracing is auto-configured by Spring Boot with Micrometer Tracing
    // This class can be used for custom tracing configuration if needed
}